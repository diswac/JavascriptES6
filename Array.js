//join array to string
const data = [100,200,300]
const result = data.join("+") // array to string ใช้ + แทน , ไม่สลายโครงสร้าง
console.log(data);
console.log(result);

//******* concat */
const data2 = ["AB", "BC", "CD"]
const allResult = data.concat(data2)
console.log(allResult);
console.log(data);